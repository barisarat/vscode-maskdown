{
  "name": "note-md-viewer",
  "displayName": "Maskdown",
  "description": "A copy-friendly Markdown note viewer for .mask.md files, with reference cards and maskable fields.",
  "version": "0.1.4",
  "publisher": "baris-dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/aratbaris/vscode-maskdown.git"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCustomEditor:noteMdViewer.viewer"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "note-md",
        "aliases": [
          "Maskdown"
        ],
        "extensions": [
          ".mask.md",
          ".note.md"
        ],
        "filenamePatterns": [
          "*.mask.md",
          "*.note.md"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "note-md",
        "scopeName": "text.html.note-markdown",
        "path": "./syntaxes/note-markdown.tmLanguage.json"
      }
    ],
    "customEditors": [
      {
        "viewType": "noteMdViewer.viewer",
        "displayName": "Maskdown",
        "selector": [
          {
            "filenamePattern": "*.mask.md"
          },
          {
            "filenamePattern": "*.note.md"
          }
        ],
        "priority": "default"
      }
    ],
    "commands": [
      {
        "command": "noteMdViewer.openAsNote",
        "title": "Open in Maskdown",
        "category": "Maskdown",
        "icon": "$(open-preview)"
      },
      {
        "command": "noteMdViewer.editSource",
        "title": "Edit Source",
        "category": "Maskdown",
        "icon": "$(edit)"
      },
      {
        "command": "noteMdViewer.copySource",
        "title": "Copy Source",
        "category": "Maskdown",
        "icon": "$(copy)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "noteMdViewer.openAsNote",
          "when": "resourceFilename =~ /\\.(note|mask)\\.md$/ && activeCustomEditorId != noteMdViewer.viewer",
          "group": "navigation@1"
        },
        {
          "command": "noteMdViewer.editSource",
          "when": "activeCustomEditorId == noteMdViewer.viewer",
          "group": "navigation@1"
        },
        {
          "command": "noteMdViewer.copySource",
          "when": "activeCustomEditorId == noteMdViewer.viewer",
          "group": "navigation@2"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "npm run compile && node test/render.test.js",
    "preview": "npm run compile && node test/preview.js",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.9.1",
    "typescript": "^5.5.0"
  }
}
